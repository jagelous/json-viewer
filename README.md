# JSON Viewer

A React app to view and explore JSON files: upload from disk or load from a URL (with optional CORS proxy).

## Run the React app

```bash
npm install
npm run dev
```

Open the URL shown (e.g. http://localhost:5173).

## Build for production

```bash
npm run build
npm run preview
```
